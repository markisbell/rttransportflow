"""rttransportflow — realtime transmission-grid simulator.

AC power flow (pandapower) + per-island frequency dynamics, runnable
standalone (accelerated tick) or as a puppet-mode game sidecar
(gamebridge contract v2). See SPEC.md at the repo root.
"""

__version__ = "0.0.1"

APP_NAME = "rttransportflow"
